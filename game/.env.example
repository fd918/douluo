# AI 服务端配置示例
# 复制为 .env.local 后再填写真实内容；不要把真实令牌提交到 Git。

# 必须以 /v1 结尾，例如：https://example.com/v1
AI_BASE_URL=

# 服务实际返回的模型 ID
AI_MODEL_ID=qwen/qwen3.6-35b-a3b

# 只允许服务端读取。不要改成 VITE_AI_API_KEY，否则可能被浏览器暴露。
AI_API_KEY=

# 单次生成超时时间，单位毫秒
AI_REQUEST_TIMEOUT_MS=90000

