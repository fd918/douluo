# 本机运营中台端口
OPS_PORT=4180

# 只允许本机访问；迁移服务器时再改为 0.0.0.0 并增加 HTTPS/身份验证
OPS_HOST=127.0.0.1

# 可选：自定义数据库与密钥目录，默认为项目内 .data
OPS_DATA_DIR=

# 公网控制层地址（不包含结尾斜杠）
OPS_CLOUD_URL=https://douluo-life-simulator.fendou913006836.chatgpt.site

# 公网中台管理凭证。只放在 .env.local，不要提交到 Git。
OPS_CLOUD_ADMIN_TOKEN=
